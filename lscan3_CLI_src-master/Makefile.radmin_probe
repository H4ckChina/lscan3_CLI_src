#define _POSIX_C_SOURCE 200809L

CC ?= cc
CFLAGS ?= -O2 -Wall -Wextra -pedantic
LDLIBS ?= -pthread

.PHONY: all clean

all: radmin_probe

radmin_probe: radmin_probe.c
	$(CC) $(CFLAGS) -std=c11 -o $@ $< $(LDLIBS)

clean:
	rm -f radmin_probe
